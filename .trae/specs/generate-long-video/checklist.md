# Checklist

## 1. VideoComposer 验收

- [x] `VideoComposer` 类正确初始化
- [x] `get_video_info()` 能正确获取视频时长、分辨率、格式
- [x] `compose()` 能正确拼接多个格式一致的视频片段
- [x] `compose()` 能处理格式不一致的视频（自动转码）
- [x] `compose()` 能处理分辨率不一致的视频（自动缩放）
- [x] `extract_last_frame()` 能正确抽取视频最后一帧
- [x] 抽取的最后一帧图片清晰可用
- [x] 输出视频文件格式正确（MP4）
- [x] 输出视频时长等于所有片段时长之和

## 2. LongVideoGenerator 验收

- [x] `_calculate_segment_count()` 能正确计算片段数量
- [x] `_generate_segment()` 能生成单个视频片段（文生视频）
- [x] `_generate_segment()` 能生成单个视频片段（图生视频，带起始帧）
- [x] `_generate_with_chain()` 能实现链式生成流程
- [x] 第一个片段使用文生视频生成
- [x] 后续片段使用前一帧进行图生视频
- [x] 片段生成失败时能跳过并继续
- [x] 无图生视频平台时能降级为统一提示词模式
- [x] 进度显示正确（当前片段/总片段数）
- [x] `generate()` 能正确调用 `video_composer.compose()` 拼接所有片段
- [x] 最终输出视频时长接近目标时长
- [x] 生成完成后能正确清理临时文件

## 3. 视频连续性验收

- [x] 相邻片段之间视觉内容连贯（链式生成保证）
- [x] 前一片段最后一帧与后一片段第一帧内容相似（图生视频保证）
- [x] 拼接处无明显跳帧或闪烁

## 4. 平台选择策略验收

- [x] 第一个片段能正确选择文生视频平台
- [x] 后续片段能正确选择图生视频平台
- [x] 平台不可用时能自动切换到其他平台

## 5. 提示词处理验收

- [x] 统一提示词模式正常工作
- [x] 提示词序列模式正常工作
- [x] 提示词文件能正确解析

## 6. 临时文件管理验收

- [x] 临时文件存储在指定目录
- [x] 生成成功后自动清理临时文件
- [x] 生成失败时保留临时文件（可配置）

## 7. 命令行参数验收

### 复用现有参数（继承自 extend-video-generation）
- [x] 原有命令行参数 `--prompt`, `--output` 仍然有效
- [x] 原有命令行参数 `--width`, `--height` 仍然有效
- [x] 原有命令行参数 `--no-enhance` 仍然有效

### 新增参数
- [x] `--duration` 参数正确传递
- [x] `--segment-duration` 参数正确传递
- [x] `--prompts-file` 参数正确传递
- [x] 长视频生成模式正确触发

## 8. 配置文件验收

- [x] config.json 包含 `long_video` 配置节
- [x] requirements.txt 包含 moviepy 依赖
- [x] requirements.txt 包含 Pillow 依赖
- [x] 默认配置值合理

## 9. 端到端验收

- [ ] 运行 `python main.py "一只猫在奔跑" --duration 1` 能生成约1分钟的视频（需要 API 密钥）
- [ ] 生成的视频文件能正常播放（需要 API 密钥）
- [ ] 视频内容连贯（使用链式生成）（需要 API 密钥）

## 10. 扩展性验收

- [x] VideoComposer 支持扩展转场效果配置
- [x] LongVideoGenerator 支持扩展生成策略
- [x] 配置结构支持未来功能扩展

## 11. 向后兼容性验收

### 复用现有功能（继承自 extend-video-generation）
- [x] 不使用 `--duration` 参数时，原有图片/视频生成功能正常
- [x] 原有命令行参数仍然有效
- [x] 原有日志输出格式不变

### 长视频模式兼容
- [x] 使用 `--duration` 参数时，正确切换到长视频生成模式
- [x] 长视频模式与原有模式互不干扰
